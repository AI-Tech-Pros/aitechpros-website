{
  "name": "orchestrateos-api",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 execute orchestrateos --local --file=../../d1/schema.sql",
    "db:seed:local": "wrangler d1 execute orchestrateos --local --file=../../d1/seed.sql",
    "db:seed:remote": "wrangler d1 execute orchestrateos --remote --file=../../d1/seed.sql"
  },
  "dependencies": {
    "hono": "^4.7.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250320.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.14.0"
  }
}
